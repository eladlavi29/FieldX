# FieldX — Vite + React + TypeScript

Quick start

1. Install dependencies:

   ``npm install``

2. Start dev server:

   ``npm run dev``

3. Build:

   ``npm run build``

4. Preview build locally:

   ``npm run preview``
